/**
 * Bootstrap the first super-admin (or invite a new admin) by creating an
 * auth.users row via the Supabase Admin API and a matching admin_users row.
 *
 * Usage:
 *   npm run create-admin -- --email andrew@progradetechlabs.com --role super_admin
 *
 * Requires SUPABASE_SERVICE_ROLE_KEY in the environment. Run locally; never
 * expose the service-role key to the client.
 *
 * Implementation deferred to Step 2 of the migration plan.
 */

throw new Error('create-admin.ts not implemented yet — see ARCHITECTURE.md §11 Step 2');
